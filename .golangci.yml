# golangci-lint configuration (v2 schema — required by golangci-lint-action v9).
#
# The default (standard) linter set, with nothing disabled. `unused` used to be
# turned off for forward-looking code that was written before it was wired up;
# that code is now either in use or deleted, so the linter runs in full.
version: "2"
